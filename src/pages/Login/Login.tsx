import React from "react";

import AppLayout from "@components/template/AppLayout";

const Login = () => {
  return (
    <AppLayout>
      <div>로그인</div>
    </AppLayout>
  );
};

export default Login;
